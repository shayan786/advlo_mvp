$('#user_cancel_reservation_modal #user_cancel_form').remove();

$('#user_cancel_reservation_modal #cancel_user_submitted').show();

// If user clicks out of the thankyou modal refresh the page
$('#user_cancel_reservation_modal').on('hide.bs.modal', function(e){
	window.location.reload(true);
})