.shared-slider-container#adventure.adventure-slider-wrapper
  .container.homepage-container
    %h1 FEATURED ADVENTURES
    %a.small_text{href: "#"} VIEW ALL >>>

  .slider-wrapper
    %i.next.fa.fa-chevron-right#adv-next
    %i.prev.fa.fa-chevron-left#adv-prev

    .swiper-container
      .swiper-wrapper
        - if @feat_adventures
          - @feat_adventures.each_with_index do |adv, i|
            %a.swiper-slide.adventure-slide{href: adventure_path(adv.slug)}
              .image-wrapper{style: "background: url(#{adv.attachment.url(:large)}) no-repeat center 0px;"}
                .copy
                  - if adv.title.length > 32
                    #title= "#{adv.title[0..32]}..."
                  -else
                    #title= adv.title

                  - if adv.location
                    - if adv.location.length > 40
                      .location= "#{adv.location.upcase[0..40]}..."
                    -else
                      .location= adv.location.upcase

                .hover-block{id: i}
                  - usr = adv.users.first
                  .guide-profile-image= image_tag "#{usr.get_avatar_url('medium')}"

                .copy-container{class: "copy-#{i}"}


:javascript
  $(window).resize(function(){
    adventureSliderInit()
  })