.shared-slider-container#adventure.adventure-slider-wrapper
  .container.homepage-container
    %h1 FEATURED ADVENTURES
    %a.small_text{href: "/adventures"} VIEW ALL >>>

  .adventure-container 
    #adventure-partial
    .brick-wrapper.masonry.js-masonry
      - @feat_adventures.each_with_index do |adv, i|
        %a.adventure-brick{href: adventure_path(adv.slug)}
          .image-wrapper{style: "background: url(#{adv.attachment.url(:large)}) no-repeat center center;"}
            .copy
              - if adv.title.length > 38
                .title= "#{adv.title[0..38]}..."
              -else
                .title= adv.title

              - if adv.location
                .location= "#{adv.city}, #{adv.state}".upcase
                

            .price= "$ #{adv.price}"

            %input{type: "hidden", id: "adv_rating_value_#{i}", "data-rating"=>adv.rating}
            %input{id: "adv_rating_show_#{i}"}
            /%input{id: "overall_rating_#{i}", type: "text", "data-rating"=>adv.rating}

            .hover-block{id: i}
              - usr = adv.users.first
              - if usr && usr.avatar_url == '' || usr && usr.avatar_url == nil
                .guide-image= image_tag "#{usr.avatar.url(:large)}"
              - elsif usr
                .guide-image= image_tag "#{usr.avatar_url}?type=large"
              - else
                .guide-image= image_tag 'missing.png'

            .copy-container{class: "copy-#{i}"}


:javascript
  $(window).resize(function(){
    adventureSliderInit()
  })