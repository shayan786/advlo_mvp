#destination-block.destination-block
  .container.homepage-container.text-center
    %h1 TOP LOCATIONS

  .row.text-center
    .container
      .region-container
        .region-wrapper
          .region 
            = link_to (image_tag HeroImage.where(region: 'Costa Rica').first.attachment.url(:hero)), '/adventures?country=Costa Rica'
            .region-text
              Costa Rica

          .region 
            = link_to (image_tag HeroImage.where(region: 'Nicaragua').first.attachment.url(:hero)), '/adventures?country=Nicaragua'
            .region-text
              Nicaragua

          .region 
            = link_to (image_tag HeroImage.where(region: 'Ecuador').first.attachment.url(:hero)), '/adventures?country=Ecuador'
            .region-text
              Ecuador

          .region 
            = link_to (image_tag HeroImage.where(region: 'Colorado').first.attachment.url(:hero)), '/adventures?state=Colorado'
            .region-text
              Colorado


          .region 
            = link_to (image_tag HeroImage.where(region: 'Washington').first.attachment.url(:hero)), '/adventures?state=Washington'
            .region-text
              Washington

          .region 
            = link_to (image_tag HeroImage.where(region: 'North America').first.attachment.url(:hero)), '/adventures?region=United States'
            .region-text
              United States

          / - @regions.each do |r|
          /   %a{href: "/adventures?location=#{r[0]}"}
          /     .region
          /       - if r.region == nil
          /         - r.region = 'default'
          /       = link_to (image_tag HeroImage.find(r.region).attachment.url(:hero)), "/adventures?city=#{r.place.gsub(' ','-')}"
          /       - adv_count = Adventure.approved.where(city: r.place).count
          /       // removed for beta cuz count is sad (#{adv_count})
          /       - if r.place.length > 14
          /         .region-text= "#{r[0][0..11]}... #{r.country}"
          /       - else 
          /         .region-text= "#{r.place},  #{r.country}"


    = render partial: "/adventures/request_modal"

:javascript
  $('#destination-block .region-wrapper').masonry({
    isFitWidth: true,
    itemSelector: '.region'
  })