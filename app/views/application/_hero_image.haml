.homepage-photo.jumbotron.jumbotron_fix{style: "background: url('#{asset_path("background/HomepageWaterfall.jpg")}') center center no-repeat"}

.scroll-slow
  #homepage-text  
    #adventure-local
      ADVENTURE LOCAL     
    #explanation
      Create and book unique adventures with local hosts around the world

  #find_adventure_form
    .col-md-6.col-md-offset-3.searchbar-background
      .form-group
        .input-group
          %input.form-control{placeholder: "Enter Destination", style: "color: #F5F5F5;", name: "location", id: "location", onfocus: "focusFunction()", onblur: "unfocusFunction()"}
          .input-group-addon
            %i.fa.fa-search

    - if notice
      .col-md-6.col-md-offset-3 
        .alert.alert-warning.text-center
          %button.close{"data-dismiss" => "alert", type: "button"}
            %span{"aria-hidden" => "true"} ×
            %span.sr-only Close
          = notice.html_safe


:javascript
  $(document).ready(function(){
    if ($.cookie('referrer_id')) {
      homepage_new_user_set_referrer_id($.cookie('referrer_id'));
    }

    homepage_find_input_geocomplete();
  });

